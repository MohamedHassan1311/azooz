import 'package:flutter/material.dart';

class AcceptConditionScreen extends StatelessWidget {
  static const routeName = 'accept_condition';

  const AcceptConditionScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
